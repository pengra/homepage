It's not unusual the need to do bulk update/create in django applications, but if you don't use the right approach your views will increase the load time to unacceptable values. Here is the common example where people starts out: But since Django 1.4 and the bulk_create built-in, insertions got a lot faster: The next examples are also executed against 10000 records Iterating over each record and manually updating the desired field: Disabling transactions and committing only once in the end of process: Update using the django built-in update()