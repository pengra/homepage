The only problem of this (and the advantage) is that the browser will handle the providen string as HTML, that means that if the HTML string contains some type of interpretable Javascript for the browser, then it will be executed: Therefore, you should use this only if you trust the source of the HTML string. If you use jQuery you can simplificate the code from the first step. The following code will do the same that the code in the first step (the warnings apply too): Our Code World is a free blog about programming, where you will find solutions to simple and complex tasks of your daily life as a developer.