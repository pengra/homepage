For example: In addition, each generated form field has attributes set as follows: Consider this set of models: Since the Author model has only 3 fields, 'name', 'title', and 'birth_date', the forms above will contain exactly the same fields. Note Note In this regard, model forms have two specific characteristics when compared to forms: There are a couple of things to note, however. Chances are these notes won't affect you unless you're trying to do something tricky with subclassing. Note As stated earlier, you can override the default queryset used by the model formset: There are three ways to render a formset in a Django template. First, you can let the formset do most of the work: Second, you can manually render the formset, but let the form deal with itself: Third, you can manually render each field: Inline formsets is a small abstraction layer on top of model formsets. These simplify the case of working with related objects via a foreign key. Suppose you have these two models: If you want to create a formset that allows you to edit books belonging to a particular author, you could do this: Note You may want to provide a view that allows a user to edit the related objects of a model. Here's how you can do that: Having trouble? We'd like to help!